// Imports

// Actions

// Actions Creators

// API Actions

// Initial State
const initialState = {};

// Reducer
function reducer(state = initialState, action) {
  switch (action.type) {
    default:
      return state;
  }
}

// Reducer Actions

// Exports

// Default Reducer Export
export default reducer;
